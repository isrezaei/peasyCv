# Frontend environment. Copy to `frontend/.env.local`:
#   cp frontend/.env.example frontend/.env.local
#
# Base URL of the NestJS backend API. Must be reachable from the browser.
NEXT_PUBLIC_API_URL=http://localhost:4000
